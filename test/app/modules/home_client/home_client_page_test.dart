import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:clinic_app/app/views/client/home/home_client_page.dart';

void main() {
  testWidgets('HomeClientPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(HomeClientPage(title: 'HomeClient')));
    //  final titleFinder = find.text('HomeClient');
    //  expect(titleFinder, findsOneWidget);
  });
}
